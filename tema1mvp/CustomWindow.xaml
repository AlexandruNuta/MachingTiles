<Window x:Class="tema1mvp.CustomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tema1mvp"
        mc:Ignorable="d"
        Title="Custom Window" Height="150" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="5">Rows:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5" x:Name="RowsTextBox" />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="5">Columns:</TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" x:Name="ColumnsTextBox" />

        <Button Grid.Row="2" Grid.Column="1" Margin="5" Content="Save" Click="SaveButton_Click" />
    </Grid>
</Window>
